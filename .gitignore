# --- Gradle / Kotlin ---
.gradle/
build/
**/build/
.kotlin/
captures/
*.hprof

# --- Configuracion local de la maquina (NUNCA se comparte) ---
local.properties
keystore.properties
*.jks
*.keystore
*.p12
*.pepk
signing/

# --- IDE ---
.idea/
*.iml
.DS_Store

# --- Salida de documentacion generada (se adjunta a la Release, no al repo) ---
docs/out/

# --- Material de tienda: artefactos y secretos ---
# El AAB y el keystore SI se generan en docs/google_play (el usuario los necesita
# en disco para subirlos), pero NO entran en git: el binario se adjunta a la
# GitHub Release y la clave de firma no puede vivir en un repositorio publico.
docs/google_play/bundle/
docs/google_play/claves/

# --- Java ---
*.class
*.log
hs_err_pid*
replay_pid*

# --- Instrucciones locales del asistente ---
.claude/
